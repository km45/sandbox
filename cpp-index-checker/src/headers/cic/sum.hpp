#ifndef CIC_SUM_HPP_
#define CIC_SUM_HPP_

namespace cic {

char Sum(char l, char r);
int Sum(int l, int r);
float Sum(float l, float r);
double Sum(double l, double r);

}  // namespace cic

#endif  // CIC_SUM_HPP_
